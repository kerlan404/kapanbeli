<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %> - Admin Panel</title>

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@400;500;600;700;800&display=swap" rel="stylesheet">

    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Inter', 'sans-serif'],
                        display: ['Poppins', 'sans-serif']
                    },
                    colors: {
                        primary: {
                            50: '#eff6ff',
                            100: '#dbeafe',
                            200: '#bfdbfe',
                            300: '#93c5fd',
                            400: '#60a5fa',
                            500: '#3b82f6',
                            600: '#2563eb',
                            700: '#1d4ed8',
                            800: '#1e40af',
                            900: '#1e3a8a'
                        },
                        accent: {
                            orange: '#FF9F43',
                            green: '#1DD1A1',
                            red: '#FF6B6B',
                            yellow: '#FECA57'
                        }
                    }
                }
            }
        };
    </script>

    <style>
        body { font-family: 'Inter', sans-serif; }
        .font-display { font-family: 'Poppins', sans-serif; }
        
        .badge { @apply px-3 py-1.5 rounded-full text-xs font-semibold inline-flex items-center gap-1.5 transition-all; }
        .badge-active { @apply bg-gradient-to-r from-green-500 to-emerald-500 text-white shadow-md shadow-green-500/30; }
        .badge-inactive { @apply bg-gradient-to-r from-gray-500 to-slate-500 text-white shadow-md shadow-gray-500/30; }
        .badge-suspended { @apply bg-gradient-to-r from-red-500 to-rose-500 text-white shadow-md shadow-red-500/30; }
        .badge-admin { @apply bg-gradient-to-r from-purple-500 to-violet-500 text-white shadow-md shadow-purple-500/30; }
        .badge-user { @apply bg-gradient-to-r from-blue-500 to-cyan-500 text-white shadow-md shadow-blue-500/30; }

        /* Activity Badge Colors */
        .badge-success { @apply bg-gradient-to-r from-green-500 to-emerald-500 text-white shadow-md shadow-green-500/30; }
        .badge-secondary { @apply bg-gradient-to-r from-gray-500 to-slate-500 text-white shadow-md shadow-gray-500/30; }
        .badge-info { @apply bg-gradient-to-r from-blue-500 to-cyan-500 text-white shadow-md shadow-blue-500/30; }
        .badge-primary { @apply bg-gradient-to-r from-primary-500 to-primary-600 text-white shadow-md shadow-primary-500/30; }
        .badge-warning { @apply bg-gradient-to-r from-yellow-500 to-amber-500 text-white shadow-md shadow-yellow-500/30; }
        .badge-danger { @apply bg-gradient-to-r from-red-500 to-rose-500 text-white shadow-md shadow-red-500/30; }

        /* Card Hover Effects */
        .stat-card { @apply relative overflow-hidden; }
        .stat-card::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(59,130,246,0.1) 0%, transparent 70%);
            opacity: 0;
            transition: opacity 0.3s;
        }
        .stat-card:hover::before { opacity: 1; }
        
        /* Custom Scrollbar */
        ::-webkit-scrollbar { width: 8px; height: 8px; }
        ::-webkit-scrollbar-track { background: #1f2937; }
        ::-webkit-scrollbar-thumb { background: #4b5563; border-radius: 4px; }
        ::-webkit-scrollbar-thumb:hover { background: #6b7280; }
    </style>
</head>
<body class="bg-gradient-to-br from-gray-950 via-gray-900 to-black min-h-screen">
    <% if (!user) { %>
        <!-- User Not Found -->
        <div class="min-h-screen flex items-center justify-center">
            <div class="text-center">
                <div class="w-24 h-24 mx-auto mb-6 bg-gradient-to-br from-red-500 to-rose-500 rounded-full flex items-center justify-center shadow-2xl shadow-red-500/40">
                    <i class="fas fa-user-times text-5xl text-white"></i>
                </div>
                <h1 class="text-3xl font-bold font-display text-white mb-3">User Tidak Ditemukan</h1>
                <p class="text-gray-400 mb-8 max-w-md">Data user yang Anda cari tidak tersedia atau telah dihapus.</p>
                <a href="/admin/users" class="inline-flex items-center gap-3 px-8 py-4 bg-gradient-to-r from-primary-600 to-primary-500 text-white rounded-xl hover:from-primary-700 hover:to-primary-600 transition-all shadow-lg shadow-primary-500/30 hover:shadow-xl hover:shadow-primary-500/40 hover:-translate-y-1 font-semibold">
                    <i class="fas fa-arrow-left"></i>
                    Kembali ke Daftar User
                </a>
            </div>
        </div>
    <% } else { %>
        <!-- Header -->
        <header class="bg-gray-900/80 backdrop-blur-xl shadow-xl border-b border-gray-800 sticky top-0 z-50">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="flex justify-between items-center py-5">
                    <div class="flex items-center gap-4">
                        <a href="/admin/users" class="group inline-flex items-center gap-2 text-gray-400 hover:text-white transition-all duration-300">
                            <div class="w-10 h-10 rounded-xl bg-gradient-to-br from-primary-500 to-primary-600 flex items-center justify-center group-hover:scale-110 transition-transform shadow-lg shadow-primary-500/30">
                                <i class="fas fa-arrow-left text-white"></i>
                            </div>
                        </a>
                        <div>
                            <h1 class="text-2xl font-bold font-display text-white flex items-center gap-3">
                                <i class="fas fa-user-circle text-primary-500"></i>
                                <%= pageTitle %>
                            </h1>
                            <p class="text-sm text-gray-400 mt-0.5">Detail lengkap profil dan aktivitas user</p>
                        </div>
                    </div>
                    <div class="flex items-center gap-3">
                        <a href="/admin/users"
                           class="inline-flex items-center gap-2 px-5 py-2.5 border border-gray-700 text-gray-300 rounded-xl hover:bg-gray-800 hover:border-gray-600 transition-all duration-300 hover:-translate-y-0.5 font-medium">
                            <i class="fas fa-users"></i>
                            <span class="hidden sm:inline">Daftar User</span>
                        </a>
                    </div>
                </div>
            </div>
        </header>

        <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
            <!-- User Info Card -->
            <div class="bg-gray-900 rounded-xl shadow-md overflow-hidden mb-8 border border-gray-800">
                <div class="p-6">
                    <div class="flex flex-col sm:flex-row items-center sm:items-start gap-6">
                        <!-- Profile Photo -->
                        <div class="flex-shrink-0">
                            <%
                                // Get profile image - use uploaded image or generate avatar
                                var profileImage = user.profile_image;
                                if (!profileImage || profileImage === 'null') {
                                    // Generate avatar from name
                                    profileImage = 'https://ui-avatars.com/api/?name=' + encodeURIComponent(user.name) + '&background=3b82f6&color=fff&size=200';
                                } else if (!profileImage.startsWith('http')) {
                                    // Local image path - make sure it's relative to public folder
                                    profileImage = profileImage.startsWith('/uploads/') ? profileImage : '/uploads/' + profileImage;
                                }
                            %>
                            <img src="<%= profileImage %>"
                                 alt="<%= user.name %>"
                                 class="w-32 h-32 rounded-full object-cover border-4 border-primary-600 shadow-lg"
                                 onerror="this.src='https://ui-avatars.com/api/?name=<%= encodeURIComponent(user.name) %>&background=3b82f6&color=fff&size=200'">
                        </div>
                        
                        <!-- User Info -->
                        <div class="flex-1 text-center sm:text-left">
                            <div class="mb-4">
                                <h2 class="text-3xl font-bold text-white mb-2"><%= user.name %></h2>
                                <p class="text-gray-400 text-lg"><i class="fas fa-envelope mr-2"></i><%= user.email %></p>
                            </div>

                            <div class="flex flex-wrap justify-center sm:justify-start gap-3 mb-4">
                                <span class="badge <%= user.role === 'admin' ? 'badge-admin' : 'badge-user' %>">
                                    <i class="fas fa-<%= user.role === 'admin' ? 'crown' : 'user' %>"></i>
                                    <%= user.role %>
                                </span>
                                <span class="badge <%= user.account_status === 'active' ? 'badge-active' : user.account_status === 'suspended' ? 'badge-suspended' : 'badge-inactive' %>">
                                    <i class="fas fa-<%= user.account_status === 'active' ? 'check-circle' : user.account_status === 'suspended' ? 'exclamation-circle' : 'times-circle' %>"></i>
                                    <%= user.account_status === 'active' ? 'Aktif' : user.account_status === 'suspended' ? 'Suspended' : 'Non-Aktif' %>
                                </span>
                            </div>

                            <div class="grid grid-cols-1 sm:grid-cols-2 gap-3 text-sm text-gray-400">
                                <div class="flex items-center gap-2">
                                    <i class="fas fa-calendar text-primary-600"></i>
                                    <span>Bergabung: <strong><%= new Date(user.created_at).toLocaleDateString('id-ID', { year: 'numeric', month: 'long', day: 'numeric' }) %></strong></span>
                                </div>
                                <div class="flex items-center gap-2">
                                    <i class="fas fa-sign-in-alt text-primary-600"></i>
                                    <span>Last Login: <strong><%= user.last_login ? new Date(user.last_login).toLocaleString('id-ID') : 'Belum pernah' %></strong></span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Statistics Grid -->
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
                <!-- Total Login -->
                <div class="bg-white rounded-xl shadow-md p-6 hover:shadow-lg transition">
                    <div class="flex items-center justify-between">
                        <div>
                            <p class="text-sm text-gray-500 mb-1">Total Login</p>
                            <p class="text-3xl font-bold text-primary-600"><%= user.total_logins || 0 %></p>
                        </div>
                        <div class="w-14 h-14 bg-primary-100 rounded-full flex items-center justify-center">
                            <i class="fas fa-sign-in-alt text-2xl text-primary-600"></i>
                        </div>
                    </div>
                </div>

                <!-- Total Products -->
                <div class="bg-white rounded-xl shadow-md p-6 hover:shadow-lg transition">
                    <div class="flex items-center justify-between">
                        <div>
                            <p class="text-sm text-gray-500 mb-1">Total Bahan</p>
                            <p class="text-3xl font-bold text-orange-600"><%= user.total_products || 0 %></p>
                        </div>
                        <div class="w-14 h-14 bg-orange-100 rounded-full flex items-center justify-center">
                            <i class="fas fa-box text-2xl text-orange-600"></i>
                        </div>
                    </div>
                </div>

                <!-- Total Notes -->
                <div class="bg-white rounded-xl shadow-md p-6 hover:shadow-lg transition">
                    <div class="flex items-center justify-between">
                        <div>
                            <p class="text-sm text-gray-500 mb-1">Total Catatan</p>
                            <p class="text-3xl font-bold text-green-600"><%= user.total_notes || 0 %></p>
                        </div>
                        <div class="w-14 h-14 bg-green-100 rounded-full flex items-center justify-center">
                            <i class="fas fa-sticky-note text-2xl text-green-600"></i>
                        </div>
                    </div>
                </div>

                <!-- Total Suggestions -->
                <div class="bg-white rounded-xl shadow-md p-6 hover:shadow-lg transition">
                    <div class="flex items-center justify-between">
                        <div>
                            <p class="text-sm text-gray-500 mb-1">Total Saran</p>
                            <p class="text-3xl font-bold text-purple-600"><%= user.total_suggestions || 0 %></p>
                        </div>
                        <div class="w-14 h-14 bg-purple-100 rounded-full flex items-center justify-center">
                            <i class="fas fa-clipboard-list text-2xl text-purple-600"></i>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Activity Logs -->
            <div class="bg-gray-900 rounded-xl shadow-md overflow-hidden mb-8 border border-gray-800">
                <div class="px-6 py-5 border-b border-gray-800 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4">
                    <div>
                        <h3 class="text-lg font-semibold text-white flex items-center gap-2">
                            <i class="fas fa-history text-primary-500"></i>
                            Log Aktivitas User
                        </h3>
                        <p class="text-sm text-gray-400 mt-1">10 aktivitas terakhir</p>
                    </div>
                    <div class="flex items-center gap-3">
                        <!-- Filter Dropdown -->
                        <select id="activityFilter" class="bg-gray-800 border border-gray-700 text-gray-300 text-sm rounded-lg focus:ring-primary-500 focus:border-primary-500 block px-4 py-2.5 hover:bg-gray-750 transition cursor-pointer">
                            <option value="all">Semua Aktivitas</option>
                            <option value="login">Login</option>
                            <option value="logout">Logout</option>
                            <option value="view">View</option>
                            <option value="create">Create</option>
                            <option value="update">Update</option>
                            <option value="delete">Delete</option>
                        </select>
                        <!-- Refresh Button -->
                        <button onclick="refreshActivityLogs()" class="inline-flex items-center gap-2 px-4 py-2.5 bg-gradient-to-r from-primary-600 to-primary-500 text-white rounded-lg hover:from-primary-700 hover:to-primary-600 transition-all shadow-lg shadow-primary-500/30 hover:shadow-xl hover:shadow-primary-500/40 hover:-translate-y-0.5">
                            <i class="fas fa-sync-alt"></i>
                            <span class="font-medium">Refresh</span>
                        </button>
                    </div>
                </div>
                <div class="overflow-x-auto">
                    <% if (activityLogs && activityLogs.length > 0) { %>
                        <table class="min-w-full divide-y divide-gray-800">
                            <thead class="bg-gray-800">
                                <tr>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">Tanggal & Waktu</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">Aktivitas</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">Deskripsi</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">IP Address</th>
                                </tr>
                            </thead>
                            <tbody class="bg-gray-900 divide-y divide-gray-800">
                                <% activityLogs.forEach(log => { 
                                    const activityType = (log.activity_type || '').toLowerCase();
                                    let badgeClass = 'badge-secondary';
                                    if (activityType === 'login') badgeClass = 'badge-success';
                                    else if (activityType === 'logout') badgeClass = 'badge-secondary';
                                    else if (activityType === 'view') badgeClass = 'badge-info';
                                    else if (activityType === 'create') badgeClass = 'badge-primary';
                                    else if (activityType === 'update') badgeClass = 'badge-warning';
                                    else if (activityType === 'delete') badgeClass = 'badge-danger';
                                    else if (activityType === 'error') badgeClass = 'badge-danger';
                                %>
                                    <tr class="hover:bg-gray-800 transition">
                                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-300">
                                            <%= new Date(log.created_at).toLocaleString('id-ID') %>
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap">
                                            <span class="badge <%= badgeClass %>">
                                                <%= log.activity_type %>
                                            </span>
                                        </td>
                                        <td class="px-6 py-4 text-sm text-gray-300 max-w-md truncate">
                                            <%= log.description %>
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-400 font-mono">
                                            <%= log.ip_address || '-' %>
                                        </td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    <% } else { %>
                        <div class="text-center py-12">
                            <i class="fas fa-inbox text-5xl text-gray-600 mb-4"></i>
                            <p class="text-gray-500">Belum ada aktivitas user</p>
                        </div>
                    <% } %>
                </div>
            </div>

            <!-- Products Section -->
            <% if (products && products.length > 0) { %>
                <div class="bg-gray-900 rounded-xl shadow-md overflow-hidden mb-8 border border-gray-800">
                    <div class="px-6 py-4 border-b border-gray-800">
                        <h3 class="text-lg font-semibold text-white flex items-center gap-2">
                            <i class="fas fa-box text-orange-500"></i>
                            Bahan/User (<%= products.length %>)
                        </h3>
                    </div>
                    <div class="overflow-x-auto">
                        <table class="min-w-full divide-y divide-gray-200">
                            <thead class="bg-gray-50">
                                <tr>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Nama Bahan</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Stok</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Min. Stok</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Kadaluarsa</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Dibuat</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y divide-gray-200">
                                <% products.forEach(product => { %>
                                    <tr class="hover:bg-gray-50">
                                        <td class="px-6 py-4 text-sm font-medium text-gray-900"><%= product.name %></td>
                                        <td class="px-6 py-4 text-sm text-gray-700">
                                            <%= product.stock_quantity %> <%= product.unit || '' %>
                                            <% if (product.stock_quantity <= 5) { %>
                                                <span class="badge bg-red-100 text-red-700 ml-2">Rendah</span>
                                            <% } %>
                                        </td>
                                        <td class="px-6 py-4 text-sm text-gray-700"><%= product.min_stock_level || 0 %> <%= product.unit || '' %></td>
                                        <td class="px-6 py-4 text-sm text-gray-700">
                                            <%= product.expiry_date ? new Date(product.expiry_date).toLocaleDateString('id-ID') : '-' %>
                                        </td>
                                        <td class="px-6 py-4 text-sm text-gray-500"><%= new Date(product.created_at).toLocaleDateString('id-ID') %></td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    </div>
                </div>
            <% } %>

            <!-- Suggestions Section -->
            <% if (suggestions && suggestions.length > 0) { %>
                <div class="bg-white rounded-xl shadow-md overflow-hidden mb-8">
                    <div class="px-6 py-4 border-b border-gray-200">
                        <h3 class="text-lg font-semibold text-gray-900 flex items-center gap-2">
                            <i class="fas fa-clipboard-list text-purple-600"></i>
                            Saran Pembelian (<%= suggestions.length %>)
                        </h3>
                    </div>
                    <div class="overflow-x-auto">
                        <table class="min-w-full divide-y divide-gray-200">
                            <thead class="bg-gray-50">
                                <tr>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Nama</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Tipe</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Deskripsi</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Status</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Dibuat</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y divide-gray-200">
                                <% suggestions.forEach(s => { %>
                                    <tr class="hover:bg-gray-50">
                                        <td class="px-6 py-4 text-sm font-medium text-gray-900"><%= s.name %></td>
                                        <td class="px-6 py-4 text-sm text-gray-700"><%= s.type %></td>
                                        <td class="px-6 py-4 text-sm text-gray-700 max-w-xs truncate"><%= s.description || '-' %></td>
                                        <td class="px-6 py-4 text-sm">
                                            <span class="badge <%= s.status === 'pending' ? 'bg-yellow-100 text-yellow-700' : 'bg-green-100 text-green-700' %>">
                                                <%= s.status || 'pending' %>
                                            </span>
                                        </td>
                                        <td class="px-6 py-4 text-sm text-gray-500"><%= new Date(s.created_at).toLocaleDateString('id-ID') %></td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    </div>
                </div>
            <% } %>

            <!-- Notes Section -->
            <% if (notes && notes.length > 0) { %>
                <div class="bg-white rounded-xl shadow-md overflow-hidden mb-8">
                    <div class="px-6 py-4 border-b border-gray-200">
                        <h3 class="text-lg font-semibold text-gray-900 flex items-center gap-2">
                            <i class="fas fa-sticky-note text-green-600"></i>
                            Catatan (<%= notes.length %>)
                        </h3>
                    </div>
                    <div class="overflow-x-auto">
                        <table class="min-w-full divide-y divide-gray-200">
                            <thead class="bg-gray-50">
                                <tr>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Judul</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Isi</th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Dibuat</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y divide-gray-200">
                                <% notes.forEach(n => { %>
                                    <tr class="hover:bg-gray-50">
                                        <td class="px-6 py-4 text-sm font-medium text-gray-900"><%= n.title %></td>
                                        <td class="px-6 py-4 text-sm text-gray-700 max-w-md truncate">
                                            <%= n.content ? n.content.substring(0, 80) + (n.content.length > 80 ? '...' : '') : '-' %>
                                        </td>
                                        <td class="px-6 py-4 text-sm text-gray-500"><%= new Date(n.created_at).toLocaleDateString('id-ID') %></td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    </div>
                </div>
            <% } %>
        </main>
    <% } %>

    <% if (user) { %>
    <script>
        // Refresh activity logs
        function refreshActivityLogs() {
            const btn = event.currentTarget;
            const icon = btn.querySelector('i');
            
            // Add spinning animation
            icon.classList.add('fa-spin');
            btn.disabled = true;
            
            // Reload page or fetch new data
            setTimeout(() => {
                location.reload();
            }, 500);
        }

        // Filter activity logs
        document.getElementById('activityFilter')?.addEventListener('change', function() {
            const filterValue = this.value;
            const rows = document.querySelectorAll('tbody tr');
            
            rows.forEach(row => {
                const activityCell = row.querySelector('td:nth-child(2)');
                if (activityCell) {
                    const activityType = activityCell.textContent.trim().toLowerCase();
                    
                    if (filterValue === 'all' || activityType === filterValue) {
                        row.style.display = '';
                    } else {
                        row.style.display = 'none';
                    }
                }
            });
        });
    </script>
    <% } %>
</body>
</html>
